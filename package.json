{
  "name": "fighter-bot",
  "version": "0.1.0",
  "description": "A Telegram bot that hunts MMA news for a watchlist of fighters and posts only what matters, with semantic dedup and an LLM matcher.",
  "license": "MIT",
  "private": true,
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --env-file-if-exists=.env server.js",
    "test": "node --test",
    "test:sql": "node --test test/sql.test.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.30.0",
    "pg": "^8.22.0"
  }
}
